---
import modelRows from "../partials/model-rows.html?raw";
---
<section class="section" id="models">
  <div class="section__inner">
  <div class="section__head"><div class="section__copy"><h2 class="h2 reveal">Thousands of models on one graph.</h2></div><p class="lead reveal" style="--d: 1;">Every model is a node with typed sockets. Wire an image into a video model, a script into a voice, a render into a 3D viewer.</p></div>
  <Fragment set:html={modelRows} />
  </div>
</section>
